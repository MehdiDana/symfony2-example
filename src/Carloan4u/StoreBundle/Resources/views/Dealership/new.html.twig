<h1>Dealership creation</h1>

<form action="{{ path('dealership_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('dealership') }}">
            Back to the list
        </a>
    </li>
</ul>
